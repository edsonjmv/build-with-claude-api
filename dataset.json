[
  {
    "task": "Parse an AWS S3 bucket name from an S3 URI like 's3://my-bucket-name/path/to/file.txt' and extract just the bucket name",
    "format": "regex"
  },
  {
    "task": "Create a JSON CloudFormation template snippet that defines an AWS Lambda function resource with basic properties (FunctionName, Runtime, Handler, Role)",
    "format": "json"
  },
  {
    "task": "Write a Python function that takes an AWS EC2 instance ID string and validates that it matches the correct format (i-followed by 8 or 17 hexadecimal characters)",
    "format": "python"
  }
]